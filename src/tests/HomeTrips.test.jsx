import HomeTrips from '../components/HomeTrips/HomeTrips';
import { describe, test, expect } from 'vitest';

describe('HomeTrips component', () => {
  test('clicking explore now button does not work', () => {});
});
