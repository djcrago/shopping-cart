import ShoppingCart from '../components/ShoppingCart/ShoppingCart';
import { describe, test, expect } from 'vitest';

describe('ShoppingCart component', () => {
  test('component accurately renders correct number of items in cart', () => {});
});
